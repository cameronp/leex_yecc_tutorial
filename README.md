# LeexYeccTutorial

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add leex_yecc_tutorial to your list of dependencies in `mix.exs`:

        def deps do
          [{:leex_yecc_tutorial, "~> 0.0.1"}]
        end

  2. Ensure leex_yecc_tutorial is started before your application:

        def application do
          [applications: [:leex_yecc_tutorial]]
        end

